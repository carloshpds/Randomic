Randomic
========

## Installation
```bash
$ cd <frontend-app-folder>
$ sudo npm install -g grunt-cli bower
$ sudo npm install
$ bower install
```

## Dev Server
```bash
$ cd <frontend-app-folder>
$ grunt server
```

It will be available at `localhost:9000`
